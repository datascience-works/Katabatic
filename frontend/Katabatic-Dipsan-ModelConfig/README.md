# Katabatic model configuration

Standalone Next.js + TypeScript implementation of the Figma model-configuration screen. It is UI-only and uses local component state; there is no backend.

## Run locally

```bash
npm install
npm run dev
```

Open <http://localhost:3001>.

## Production

```bash
npm run build
npm start
```
